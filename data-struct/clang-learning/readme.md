结合你的 **TS 开发背景** + **408考研C语言刚需**，我分**3个难度梯度**设计了**9个精准练习需求**，完全贴合考试要求，**无高级特性**，从基础语法到数据结构核心代码全覆盖，你写完可以直接对照校验！

---

# 第一阶段：基础语法巩固（无指针，入门必练）
目标：熟练C语言变量、数组、循环、函数、值传递（和TS语法高度一致）
## 练习1：一维数组遍历求和
### 需求
1. 定义一个长度为5的整型数组，手动赋值 `{1,2,3,4,5}`
2. 用 `for` 循环遍历数组，计算所有元素的和
3. 用 `printf` 输出结果
### 考点
数组、循环、输出、基础运算

## 练习2：查找数组中的最大值
### 需求
1. 定义任意整型数组（长度自定）
2. 编写函数 `int getMax(int arr[], int n)`，返回数组最大值
3. 在`main`函数中调用并输出结果
### 考点
函数定义、数组传参、条件判断

## 练习3：函数值传递测试
### 需求
1. 定义函数 `void change(int x)`，函数内把`x`赋值为100
2. `main`函数中定义变量`a=10`，调用`change(a)`
3. 输出调用后的`a`，观察值是否改变
### 考点
C语言值传递特性（考研易错点）

---

# 第二阶段：核心进阶（指针+结构体，考研必考）
目标：掌握**指针、地址传递、结构体**（数据结构链表/树的基础）
## 练习4：指针实现两个整数交换
### 需求
1. 用**指针传参**编写交换函数 `void swap(int *a, int *b)`
2. 主函数定义两个变量，调用函数交换值
3. 输出结果
### 考点
取地址`&`、解引用`*`、地址传递（TS无此语法，核心难点）

## 练习5：结构体存储学生信息
### 需求
1. 定义结构体`Student`，包含：学号(`int`)、姓名(`char[10]`)、分数(`int`)
2. 定义结构体变量，赋值并打印所有信息
### 考点
结构体定义、成员访问（`.`）

## 练习6：指针操作数组
### 需求
1. 定义数组`int arr[] = {10,20,30}`
2. 定义指针指向数组首地址
3. 用指针遍历数组，修改所有元素为原来的2倍
### 考点
指针与数组的关系、指针遍历

---

# 第三阶段：数据结构实战（malloc+顺序表+链表，408核心）
目标：能写出考研数据结构标准C代码（考试100%考）
## 练习7：动态内存分配（创建整型数组）
### 需求
1. 用`malloc`申请5个int大小的动态数组
2. 赋值、遍历、输出
3. 用`free`释放内存
### 考点
`malloc`、`free`、动态内存（TS`new`的底层实现）

## 练习8：顺序表初始化（408必考）
### 需求
1. 定义顺序表结构体（固定长度数组+长度变量）
2. 编写初始化函数：将长度置为0
3. 主函数调用初始化
### 考点
结构体指针、`->`操作符、数据结构标准写法

## 练习9：单链表节点创建（408必考）
### 需求
1. 定义单链表节点结构体（数据域+指针域）
2. 用`malloc`创建一个节点，赋值数据，指针置`NULL`
3. 打印节点数据
### 考点
链表节点定义、动态创建节点、`NULL`使用

---

# 练习规则（严格遵循考研C规范）
1. 所有代码必须有`main`函数，且`return 0`
2. 不使用C语言高级特性（枚举、宏、函数指针等）
3. 变量先定义后使用，格式整洁
4. 对标你之前的C语言学习文档编写

---

你可以**逐个完成**，写完后把代码发给我，我帮你：
✅ 校验语法错误
✅ 修正考研不规范写法
✅ 对照TS帮你理解底层逻辑
✅ 优化成标准408答题代码！